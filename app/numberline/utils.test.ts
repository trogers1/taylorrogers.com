import { describe, it, expect } from 'vitest';

describe('numberLine utils', () => {
  describe('calculatePercentage', () => {
    it('should correctly calculate the percentage relative to various min and maxes', () => {});
  });
  describe('organizeLabels', () => {});
});
